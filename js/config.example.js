/* ============================================================
   Copy this file to js/config.js for local development.
   js/config.js is in .gitignore — never commit real keys.
   Get values from: https://supabase.com/dashboard → Settings → API
============================================================ */
window.__MP_CONFIG = {
  supabaseUrl:  'https://YOUR_PROJECT_ID.supabase.co',
  supabaseKey:  'YOUR_ANON_PUBLIC_KEY',
  adminSecret:  'markuspro2026'
};
